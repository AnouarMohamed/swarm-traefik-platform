version: '3.8'

services:
  wp:
    image: registry.example.com/platform/wordpress:6.9.2-php8.3-r1
    env_file:
      - .env
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.wordpress-invoice-wp.rule=Host(`wordpress-invoice.example.com`)"
        - "traefik.http.services.wordpress-invoice-wp.loadbalancer.server.port=80"
        - "traefik.http.routers.wordpress-invoice-wp.tls=true"
        - "traefik.http.routers.wordpress-invoice-wp.tls.certresolver=leresolver"
        - "traefik.http.routers.wordpress-invoice-wp.entrypoints=websecure"
        - "traefik.http.routers.wordpress-invoice-wp.middlewares=authinfra"
    volumes:
      - wordpress-invoice:/var/www/html
    networks:
      - edge-net
      - mysql-net


volumes:
  wordpress-invoice:
    external: true

networks:
  edge-net:
    external: true
  mysql-net:
    external: true
