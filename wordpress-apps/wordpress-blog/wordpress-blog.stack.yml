version: '3.8'

services:
  wp:
    image: registry.example.com/platform/wordpress:6.9.2-php8.3-r1
    env_file:
      - .env
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.wordpress-blog.rule=Host(`wordpress-blog.example.com`)"
        - "traefik.http.services.wordpress-blog.loadbalancer.server.port=80"
        - "traefik.http.routers.wordpress-blog.tls=true"
        - "traefik.http.routers.wordpress-blog.tls.certresolver=leresolver"
        - "traefik.http.routers.wordpress-blog.entrypoints=websecure"
    volumes:
      - wordpress-blog:/var/www/html
    networks:
      - edge-net
      - mysql-net


volumes:
  wordpress-blog:
    external: true

networks:
  edge-net:
    external: true
  mysql-net:
    external: true
